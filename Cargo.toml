[workspace]
resolver = "2"
members = [
    "syncstorage-db-common",
    "syncstorage-common",
    "tokenserver-common",
    "syncstorage",
]
default-members = ["syncstorage"]

[profile.release]
# Enables line numbers in Sentry reporting
debug = 1
